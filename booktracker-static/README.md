# Book Tracker — static UI representation

This folder is a static, front-end representation of the original Django Book Tracker application. It preserves the original visual language, page structure, and representative interactions while leaving the original Django implementation untouched.

There is no backend, authentication service, database, or persistence here. Login/signup lead into a dummy dashboard, search/filtering and forms run in vanilla JavaScript, and book data is sample browser-side data.
